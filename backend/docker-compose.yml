services:
  api:
    image: ${ECR_IMAGE}
    restart: unless-stopped
    ports:
      - "80:4000"
    environment:
      PORT: 4000
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN}
